services:
  nginx:
    image: nginx:latest
    ports:
      - "8086:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    environment:
      - APP_VERSION="blue"
    depends_on:
      - ml-service-blue
      - ml-service-green